// guess result types
export type NameAge = {
    name: string;
    age: number;
}

// user form input type
export type UserFormData = {
    name: string;
}

// back-end api response type
export type ApiResponse = {
    name: string
    age: number
    count: number
}
